document.querySelectorAll('path').forEach(e => e.onclick = () => alert(`ID Региона: ${e.getAttribute('id')}`))
// document.querySelectorAll('path').forEach(e => e.onclick = () => alert(`ID Региона:`))